{
  "name": "nwc-bridge",
  "version": "1.0.0",
  "description": "NWC Bridge - HTTP GET endpoint for Lightning payments via Nostr Wallet Connect",
  "private": true,
  "scripts": {
    "dev": "vercel dev",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@getalby/sdk": "^7.0.0",
    "websocket-polyfill": "^0.0.3"
  },
  "devDependencies": {
    "vercel": "^39.0.0"
  }
}
